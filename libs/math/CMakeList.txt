cmake_minimum_required(VERSION 3.23)

if(CMAKE_SOURCE_DIR STREQUAL CMAKE_CURRENT_SOURCE_DIR)
  project(math LANGUAGES CXX)
endif()

include(${CMAKE_CURRENT_LIST_DIR}/../monorepo.cmake)

monorepo_add_library(math HEADER_ONLY
  CXX_STANDARD 20
)